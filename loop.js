/*import React from "react";
import {
  BrowserRouter as Router,
  Switch,
  Route,
  Link,
  useParams,
  useRouteMatch
} from "react-router-dom";
import songList from './songs'

/*function loop() {
const items = songList.map(song => `<li><Link to={`${url}/rendering`}>Rendering with React</Link></li>`)
}*/

 /*export function LoopSong(songList)
{   let { path, url } = useRouteMatch();
    return (
    songList.map(song => <li><Link to={`${url}/songList.title`}>{songList.title}</Link></li>)
    )
}
*/